////////////////////////////////////////////////////////////////////////////////////////
//
// Constants
//
var OTHER = Math.pow(2, 1),
  BODY_CENTER = Math.pow(2, 2),
  BODY_LEFT = Math.pow(2, 3),
  BODY_RIGHT = Math.pow(2, 4),
  OTHER_RAGDOLL = Math.pow(2, 5),
  GROUND = Math.pow(2, 6),
  OTHER = Math.pow(2, 7)
